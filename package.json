{
  "name": "surface",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run transpile && parcel index.html --open",
    "build": "npm run transpile && parcel build index.html -d build --public-url .",
    "transpile": "node-sass ./src/css/styles.scss ./src/css/styles.css",
    "deploy": "gh-pages -d build",
    "predeploy": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/max-razhnov/surface.git"
  },
  "author": "max razhnov",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/max-razhnov/surface/issues"
  },
  "dependencies": {
    "node-sass": "^4.13.1"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.3"
  }
}
